"""ballast: a leveraged-ETF rebalancing bot.

The :mod:`ballast.core` package holds the pure domain layer: immutable types,
the strategy protocol, the instrument registry, and the config schema.
"""

__version__ = "0.1.0"
